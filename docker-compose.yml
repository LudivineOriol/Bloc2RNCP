services:
  db:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: Takamine0308!
      MYSQL_DATABASE: patissons
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql

volumes:
  mysql_data:
